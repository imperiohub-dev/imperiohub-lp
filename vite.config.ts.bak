import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { resolve } from 'path'
import dts from 'vite-plugin-dts'

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(),
    dts({
      include: ['src'],
      exclude: ['src/**/*.test.ts', 'src/**/*.test.tsx', 'src/**/*.spec.ts', 'src/**/*.spec.tsx'],
      insertTypesEntry: true,
      outDir: 'dist',
      entryRoot: 'src',
      // Genera .d.ts preservando la estructura de carpetas
      rollupTypes: false,
    }),
  ],
  build: {
    lib: {
      // Múltiples entry points para exports modulares
      entry: {
        'index': resolve(__dirname, 'src/index.ts'),
        'schemas/index': resolve(__dirname, 'src/schemas/index.ts'),
        'components/index': resolve(__dirname, 'src/components/index.ts'),
        'generator/index': resolve(__dirname, 'src/generator/index.ts'),
      },
      formats: ['es', 'cjs'],
    },
    rollupOptions: {
      external: ['react', 'react-dom', 'react/jsx-runtime', '@prisma/client', 'zod'],
      output: [
        {
          format: 'es',
          entryFileNames: '[name].js',
          preserveModules: false,
          globals: {
            react: 'React',
            'react-dom': 'ReactDOM',
            'react/jsx-runtime': 'react/jsx-runtime',
          },
        },
        {
          format: 'cjs',
          entryFileNames: '[name].cjs',
          preserveModules: false,
          globals: {
            react: 'React',
            'react-dom': 'ReactDOM',
            'react/jsx-runtime': 'react/jsx-runtime',
          },
        },
      ],
    },
    sourcemap: true,
    emptyOutDir: true,
  },
})
